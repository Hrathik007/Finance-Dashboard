package com.upwise.backend.controller;

// Debug endpoints removed for cleanliness. This file is retained for history but no longer
// registers any Spring MVC endpoints or beans. If you need temporary debug endpoints,
// recreate them in a separate development-only branch protected by @Profile("dev").

public final class DebugController {
    // Debug controller intentionally disabled to keep production surface clean.
}
